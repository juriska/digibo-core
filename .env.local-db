# Local Development with Local Oracle DB (Docker)
PORT=3000
NODE_ENV=development
MOCK_ENABLED=false

# Local Docker Oracle XE Configuration
DB_USER=system
DB_PASSWORD=MySecurePass123
DB_CONNECTION_STRING=localhost:1521/XEPDB1